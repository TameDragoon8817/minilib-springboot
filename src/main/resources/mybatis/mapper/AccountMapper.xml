<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mavenbase.minilibspringboot.dao.AccountMapper">
<select id="getAccount" resultType="com.mavenbase.minilibspringboot.pojo.Account">
    select * from book.securityusers where name = #{name}
</select>
</mapper>